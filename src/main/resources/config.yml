# distance settings
settings:
  default-range: 50
  max-frequency: 12000
  min-frequency: 8000

# ItemsAdder blocks list (Namespace:ID)
blocks:
  transmitters:
    - "JUKEBOX"
  receivers:
    - "IRON_BLOCK"
